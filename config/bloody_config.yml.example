title: "Bloody CMS"			# The name used in titles 
header: "header.png"		# The image to show in the header

sidebar:
  blocks:
    example_block1: ''	# These blocks will be rendered int he sidebar if the page is wide enough (> 800px)

home:
  enabled: true         # wether to show the sidebar home module
  in_nav: true          # wether to actually show it in the sidebar
  sidebar_title: "home" # the title for in the sidebar

blog:
  enabled: true         #toggles wether this feature is enabled
  in_nav: true          #toggles wether this feature is shown in the sidebar
  title: "blog"         #the title for the sidebar
  path: "blog"          #the path used in the url (e.g. http://example.com/your_path/post_name)

pages:
  enabled: true         #toggles wether this feature is enabled
  in_nav: true          #toggles wether this feature is shown in the sidebar
  # root_page: "home"   #the root page to show instead of the blog
  
twitter:
  consumer_key: ''			# Your twitter authentication key (used for login)
  onsumer_secret: ''		# Your twitter authentication secret  