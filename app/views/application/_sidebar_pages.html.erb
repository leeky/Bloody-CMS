<% pages.each do |page| %>
	<% class_name = params[:controller] == 'pages' && params[:id] == page.slug ? 'active' : '' %>
	<% 
		name = page.sidebar_title_or_title 
		name += " (unpublished)" if !page.published? %>
	<%= link_to name, root_page_path(page), :class => class_name  %>
<% end %>