<% @posts = Post.published.descending_date.limit(Option.get("blog:recent_count")) %>

<% if @posts.count > 0 %>
	<header><%= Option.get("blog:title") %></header>
	<%= render :partial => @posts, :locals => {:show_nav => false, :highlight_unpublished => false} %>
<% end %>
