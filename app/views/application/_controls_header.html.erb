<nav id='controls'>
	<ul>
		<li>
			<%= link_to "Logout", "/logout", :class => "button medium white" if logged_in? %>
			<%= link_to image_tag("twitter_connect.gif"), "/login" if !logged_in? %>	
		</li>
	</ul>
</nav>