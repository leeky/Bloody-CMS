<% @posts = Post.published.descending_date.limit(Settings.get("blog:recent_count")) %>

<% if @posts.count > 0 %>
	<header><%= Settings.get("blog:title") %></header>
	<%= render :partial => @posts, :locals => {:show_nav => false, :highlight_unpublished => false} %>
<% end %>
