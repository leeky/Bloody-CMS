<% 
	events = admin? ? Event : Event.published
	@events = events.coming_up.by_date_asc.limit(Settings.get("events:show_upcoming_events")) %>

<% if @events.count > 0 %>
		<%= render :partial => @events, :locals => {:show_nav => true, :highlight_unpublished => true, :small => true} %>
<% end %>
<br/>
