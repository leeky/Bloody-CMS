<% pages = admin? ? Page.in_sidebar : Page.in_sidebar.published %>

<% pages.each do |page| %>
	<% class_name = request.path == page.slug ? 'active' : '' %>
	<%= link_to page.sidebar_title_or_title, root_page_path(page), :class => class_name  %>
<% end %>